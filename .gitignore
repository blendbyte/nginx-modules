# Build outputs
build/
artifacts/
*.deb
*.dsc
*.tar.gz
*.tar.xz
*.changes
*.buildinfo

# Resolved refs cache (generated by resolve-refs.sh)
.resolved-refs.yaml

# Build serial tracking (CI-managed, but kept out of dev clones)
.build-serials

# GPG keys — NEVER commit
*.asc
*.gpg
!**/blendbyte-archive-keyring.gpg.example
secrets/

# Editor / OS cruft
.DS_Store
.idea/
.vscode/
*.swp
*~
